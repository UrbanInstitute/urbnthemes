% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_urbn_logo.R
\name{add_urbn_logo}
\alias{add_urbn_logo}
\title{Add Urban Institute logo to a ggplot}
\usage{
add_urbn_logo(
  plot,
  logo_path = system.file("urbn-logo.png", package = "urbnthemes"),
  position = "right",
  logo_height = 0.08
)
}
\arguments{
\item{plot}{A ggplot2 object}

\item{logo_path}{Path to logo PNG file. Defaults to the package logo.}

\item{position}{Horizontal position of logo: "right" (default) or "left"}

\item{logo_height}{Height of logo row relative to plot. Default is 0.08 (8\% of plot height).}
}
\value{
A patchwork/ggplot2 object with the logo added
}
\description{
Adds the Urban Institute logo below a ggplot object using patchwork.
The logo should be placed in \code{inst/urbn-logo.png} within the package.
}
\details{
This function requires the \code{png} and \code{patchwork} packages.

The logo is added as a separate row below the plot, so it does not overlap
with the visualization.
}
\examples{
\dontrun{
library(ggplot2)
library(urbnthemes)

p <- ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  theme_urbn_print()

add_urbn_logo(p)
}

}
