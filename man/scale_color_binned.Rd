% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scales.R
\name{scale_color_binned}
\alias{scale_color_binned}
\alias{scale_colour_binned}
\title{Binned color scale that aligns with the Urban Institute style}
\usage{
scale_color_binned(
  palette = "cyan",
  n.breaks = NULL,
  nice.breaks = TRUE,
  na.value = "grey50",
  guide = "coloursteps",
  ...
)

scale_colour_binned(
  palette = "cyan",
  n.breaks = NULL,
  nice.breaks = TRUE,
  na.value = "grey50",
  guide = "coloursteps",
  ...
)
}
\arguments{
\item{palette}{Name of the Urban Institute palette to use. Options:
"cyan" (default), "gray", "yellow", "magenta", "green",
"spacegray", "red", "main"}

\item{n.breaks}{Approximate number of breaks/bins to create. If NULL, uses
ggplot2's default binning (5 breaks). When \code{nice.breaks = TRUE}
(the default), breaks are rounded to nice values, so the realized bin
count may not exactly equal \code{n.breaks + 1}.}

\item{nice.breaks}{Should breaks be rounded to nice values? Default TRUE.}

\item{na.value}{Color to use for missing values}

\item{guide}{Type of legend. defaults to "coloursteps"}

\item{...}{other arguments passed to \code{binned_scale()}}
}
\description{
Creates discrete color bins for continuous data. Useful for creating
choropleth maps or binned heatmaps that follow Urban Institute style.
}
